# 🚀 Быстрый старт за 5 минут

## 1. Создайте бота в Telegram

1. Откройте [@BotFather](https://t.me/BotFather)
2. Отправьте `/newbot`
3. Придумайте имя: `Твоя Линия Dating`
4. Придумайте username: `tvoyalinia_bot` (или другой)
5. Скопируйте **токен** (выглядит как `123456:ABC-DEF...`)

## 2. Настройте проект

```bash
# Клонируйте или перейдите в директорию проекта
cd /workspace

# Установите зависимости
npm install

# Создайте .env файл
cp .env.example .env
```

**Откройте `.env` и вставьте:**
- `BOT_TOKEN` — ваш токен от BotFather
- `ADMIN_IDS` — ваш Telegram ID (узнать: [@userinfobot](https://t.me/userinfobot))

Пример:
```env
BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
ADMIN_IDS=123456789
```

## 3. Инициализируйте базу данных

```bash
# Создаст database.db и применит миграции
npm run prisma:migrate
```

При запросе имени миграции введите: `init`

## 4. Запустите бота

```bash
npm run dev
```

Вы увидите:
```
✅ Bot started successfully
```

## 5. Протестируйте

1. Найдите вашего бота в Telegram: `@tvoyalinia_bot` (ваш username)
2. Нажмите **Start**
3. Бот должен ответить: "Привет! 👋 Бот в разработке..."

---

## 🎉 Готово!

Теперь можно приступать к разработке функций по [Roadmap](./ROADMAP.md).

---

## 🔧 Полезные команды

```bash
# Разработка с hot-reload
npm run dev

# Production запуск
npm start

# Открыть Prisma Studio (GUI для БД)
npm run prisma:studio

# Создать новую миграцию после изменения schema.prisma
npm run prisma:migrate
```

---

## 🐛 Проблемы?

### Бот не отвечает
- Проверьте, что `BOT_TOKEN` правильный
- Убедитесь, что бот запущен (`npm run dev`)
- Проверьте логи в консоли

### Ошибка БД
```bash
# Пересоздать БД
rm -rf data/database.db
npm run prisma:migrate
```

### Telegram API rate limit
- Подождите 1 минуту
- Не отправляйте много запросов подряд

---

## 📚 Следующие шаги

1. Изучите [MVP_SPEC.md](./MVP_SPEC.md) — полная спецификация
2. Следуйте [ROADMAP.md](./ROADMAP.md) — план разработки
3. Читайте комментарии в коде — там подсказки
4. Смотрите примеры в `/src` — уже готовые утилиты

---

**Вопросы?** Пишите в Issues или читайте документацию [grammy](https://grammy.dev) и [Prisma](https://prisma.io).
