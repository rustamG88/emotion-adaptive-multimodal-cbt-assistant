# MVP «Твоя Линия» — минимальная версия (3-4 недели)

## 🎯 Цель MVP
Проверить гипотезу: **нужен ли Telegram-бот знакомств для плюс-сайз женщин с фокусом на безопасность и уважение?**

**Критерий успеха:** 100+ активных анкет, 50+ матчей, 10+ платежей за первый месяц после запуска.

---

## ✂️ Что ВЫРЕЗАЛИ из полного ТЗ

❌ YooKassa (только Telegram Stars)  
❌ ML-модерация токсичности (только стоп-слова)  
❌ Видео-верификация (только фото)  
❌ Множественные подборки (только базовый поиск)  
❌ Icebreakers и готовые фразы  
❌ Реферальная программа  
❌ Городские бусты  
❌ "Кто смотрел меня"  
❌ Детальная аналитика  
❌ Промокоды и акции  

---

## 📦 Что ВХОДИТ в MVP

### 1️⃣ Онбординг (30 сек)
**Шаги:**
1. Приветствие + политика 18+ (согласие)
2. Роль: `Девушка / Парень / Другое`
3. Цели: `Семья / Свидания / Дружба / Общение` (множественный выбор)
4. Город (текст или геолокация)
5. Возраст (число)

**Состояния FSM:**
- `ONBOARDING_START`
- `ONBOARDING_ROLE`
- `ONBOARDING_GOALS`
- `ONBOARDING_LOCATION`
- `ONBOARDING_AGE`
- `ONBOARDING_DONE` → переход в создание анкеты

---

### 2️⃣ Анкета (минимум)
**Обязательные поля:**
- 1-3 фото (главное + доп.)
- О себе (до 300 символов)
- Рост (см)
- Интересы (5 тегов из списка: кофе, кино, спорт, путешествия, книги, музыка, природа, кулинария, танцы, игры)

**Опциональные:**
- Дети (есть/нет/планирую)
- Язык(и) (RU по умолчанию)

**Приватность:**
- Показывать город: да/нет
- Радиус поиска: 10/25/50/100 км

**Inline-меню анкеты:**
```
[ 📸 Фото ] [ ✍️ О себе ]
[ 🎯 Интересы ] [ 📍 Гео ]
[ ✅ Готово ] [ 👁 Просмотр ]
```

---

### 3️⃣ Поиск (простой)
**Один режим: «Все рядом»**

**Фильтры (inline):**
- Возраст: от/до (ползунки или кнопки 18-25, 25-35, 35-45, 45+)
- Цели: пересечение с моими целями
- Радиус: из настроек анкеты

**Сортировка:**
- Сначала верифицированные
- Потом по активности (недавние онлайн)
- Потом случайные

**Inline-кнопки карточки:**
```
[ 💬 Написать ] [ ❤️ Интересно ]
[ 👎 Пропустить ] [ ⚠️ Пожаловаться ]
```

**Логика скоринга (упрощённая):**
```
score = 
  + 10 (если пересечение целей)
  + 5 (за каждый общий интерес, макс 25)
  + 20 (если верифицирован)
  - штраф за расстояние (0-10 км = 0, 100+ км = -20)
```

---

### 4️⃣ Чат (базовый)
- Чат внутри бота через `sendMessage` с `reply_markup`
- Можно отправить: текст, фото, голос
- **Лимиты без матча:** 1 сообщение (премиум: 5)
- Матч = взаимное "Интересно"

**Inline-кнопки в чате:**
```
[ 📎 Фото ] [ 🚫 Заблокировать ] [ ⬅️ К поиску ]
```

**Хранение:**
- SQLite таблица `messages`: id, from_user, to_user, text, media_file_id, created_at
- Последние 100 сообщений на пару

---

### 5️⃣ Верификация (базовая)
**Только фото:**
- Попросить селфи с жестом (✌️ или 👍)
- Ручная проверка админом/модератором
- Статусы: `pending`, `approved`, `rejected`
- Бейдж `✔️ Проверено` в карточке

**Можно отложить:** кнопка "Позже" → напоминание через 24ч

---

### 6️⃣ Безопасность (минимум)
**Жалобы:**
- Быстрый репорт: `Фейк / Шейминг / Домогательства / Спам`
- Админ получает уведомление + карточка нарушителя
- Действия: `Предупредить / Бан / Удалить профиль`

**Стоп-слова (регэкспы):**
- Список из 20-30 токсичных фраз про вес/тело
- Авто-блок сообщения + уведомление админу

**Rate limits:**
- Макс 10 исходящих сообщений в час (без матча)
- Макс 3 жалобы от одного юзера в день

---

### 7️⃣ Премиум (Stars only)
**Один тариф: "Premium" (300 Stars/мес ≈ 200₽)**

**Что даёт:**
- 5 сообщений до матча (вместо 1)
- Бейдж `⭐ Premium` в карточке
- Приоритет в поиске (+10 к скорингу)

**Интеграция:**
- `sendInvoice` с `currency: "XTR"`
- Обработка `pre_checkout_query` и `successful_payment`
- Записываем в БД: `premium_until` (timestamp)

**Разовые услуги (опционально в MVP):**
- `⭐ Супер-интерес` (50 Stars): уведомление + повышенный вес

---

### 8️⃣ Админ-панель (минимум)
**Доступ:** по `ADMIN_IDS` из `.env`

**Функции:**
```
[ 📊 Статистика ]
  - Всего пользователей
  - Анкет заполнено
  - Верифицированных
  - Платежей за месяц
  - Жалоб открытых

[ 🧹 Модерация ]
  - Очередь верификации фото
  - Очередь жалоб
  - Быстрые действия: Одобрить / Отклонить / Бан

[ 💵 Платежи ]
  - Список транзакций Stars
  - Ручное начисление премиума (для тестов)
```

**Клавиатура:**
```
[ 📊 Стат ] [ 🧹 Модер ] [ 💵 Деньги ]
[ ⬅️ Выход из админки ]
```

---

## 🗄️ База данных (SQLite + Prisma)

**Почему не JSON:**
- Поиск по фильтрам = сканирование всех файлов
- Race conditions при записи
- Нет индексов, транзакций, связей

**Схема Prisma:**

```prisma
// schema.prisma

datasource db {
  provider = "sqlite"
  url      = "file:./data/database.db"
}

generator client {
  provider = "prisma-client-js"
}

model User {
  id            Int       @id @default(autoincrement())
  tgId          String    @unique
  role          String    // female, male, other
  age           Int
  city          String
  lat           Float?
  lon           Float?
  radiusKm      Int       @default(50)
  showCity      Boolean   @default(true)
  
  goals         String    // JSON: ["family", "dating"]
  interests     String    // JSON: ["coffee", "cinema"]
  
  height        Int?
  bio           String?
  photos        String    // JSON: ["file_id1", "file_id2"]
  mainPhoto     String?
  
  verifiedAt    DateTime?
  verifiedBy    Int?
  
  premiumUntil  DateTime?
  
  reputation    Float     @default(0)
  reportCount   Int       @default(0)
  isBanned      Boolean   @default(false)
  
  createdAt     DateTime  @default(now())
  updatedAt     DateTime  @updatedAt
  lastOnline    DateTime  @default(now())
  
  sentMessages     Message[] @relation("SentMessages")
  receivedMessages Message[] @relation("ReceivedMessages")
  sentReports      Report[]  @relation("Reporter")
  receivedReports  Report[]  @relation("Reported")
  
  @@index([city, age, isBanned])
}

model Message {
  id          Int      @id @default(autoincrement())
  fromUserId  Int
  toUserId    Int
  text        String?
  mediaFileId String?
  mediaType   String?  // photo, voice, video
  
  createdAt   DateTime @default(now())
  
  fromUser User @relation("SentMessages", fields: [fromUserId], references: [id])
  toUser   User @relation("ReceivedMessages", fields: [toUserId], references: [id])
  
  @@index([fromUserId, toUserId, createdAt])
}

model Match {
  id        Int      @id @default(autoincrement())
  user1Id   Int
  user2Id   Int
  createdAt DateTime @default(now())
  
  @@unique([user1Id, user2Id])
}

model Like {
  id        Int      @id @default(autoincrement())
  fromId    Int
  toId      Int
  isSuperLike Boolean @default(false)
  createdAt DateTime @default(now())
  
  @@unique([fromId, toId])
}

model Report {
  id          Int      @id @default(autoincrement())
  reporterId  Int
  reportedId  Int
  type        String   // fake, shaming, harassment, spam
  evidence    String?  // JSON: {message_id, photo_id}
  status      String   @default("open") // open, resolved, dismissed
  
  createdAt   DateTime @default(now())
  resolvedAt  DateTime?
  resolvedBy  Int?
  action      String?  // warn, mute, ban, delete
  
  reporter User @relation("Reporter", fields: [reporterId], references: [id])
  reported User @relation("Reported", fields: [reportedId], references: [id])
  
  @@index([status, createdAt])
}

model Payment {
  id          Int      @id @default(autoincrement())
  userId      Int
  provider    String   // stars
  product     String   // premium_month, super_like
  amount      Int
  currency    String   // XTR
  status      String   // pending, paid, failed
  
  telegramPaymentId String? @unique
  
  createdAt   DateTime @default(now())
  paidAt      DateTime?
  
  @@index([userId, status])
}

model Admin {
  id        Int      @id @default(autoincrement())
  tgId      String   @unique
  role      String   // owner, admin, moderator
  createdAt DateTime @default(now())
}
```

---

## 🏗️ Техстек

**Backend:**
- Node.js 20+
- grammy (современная альтернатива Telegraf)
- Prisma ORM
- SQLite (для MVP; миграция на PostgreSQL при росте)

**Структура проекта:**
```
/workspace
  /src
    /bot
      handlers/       # Обработчики команд/callback
      middlewares/    # FSM, auth, rate-limit
      keyboards/      # Inline-клавиатуры
    /services
      user.service.js
      search.service.js
      payment.service.js
      moderation.service.js
    /utils
      scoring.js
      stopwords.js
      geo.js
    bot.js            # Инициализация бота
    index.js          # Точка входа
  /prisma
    schema.prisma
    migrations/
  /data
    database.db
  .env
  package.json
  README.md
```

---

## 📱 Навигация (inline)

**Главное меню (после заполнения анкеты):**
```
[ 🔎 Поиск ] [ 👤 Моя анкета ]
[ ⭐ Премиум ] [ ⚙️ Настройки ]
```

**Моя анкета:**
```
[ 📸 Изменить фото ] [ ✍️ Изменить текст ]
[ 🎯 Интересы ] [ 🛡️ Верификация ]
[ 🗑 Удалить анкету ] [ ⬅️ Назад ]
```

**Поиск:**
```
[ 🎲 Случайная анкета ]
[ 🎯 Фильтры: возраст, цели ]
[ ⬅️ Главное меню ]
```

**Карточка в поиске:**
- Фото(а)
- Имя, возраст, город (если не скрыт)
- О себе
- Интересы (теги)
- Бейджи: `✔️ Проверено`, `⭐ Premium`

```
[ 💬 Написать ] [ ❤️ Интересно ]
[ 👎 Пропустить ] [ ⚠️ Репорт ]
```

---

## 🔐 Безопасность

**Минимальный набор:**
1. **18+ чек:** согласие при старте (текст + кнопка)
2. **Стоп-слова:** 30 фраз про вес/шейминг → авто-блок + алерт админу
3. **Rate limits:** 
   - 10 сообщений/час без матча
   - 20 просмотров анкет/час
   - 3 жалобы/день от одного юзера
4. **Жалобы:** быстрая модерация (1-24ч)
5. **Баны:** 
   - 1 нарушение = предупреждение
   - 2 нарушения = мут 24ч
   - 3+ = бан

**Стоп-слова (примеры):**
```js
const STOPWORDS = [
  /жирн(ая|ую|ой)/gi,
  /толст(ая|ую|ой)/gi,
  /корова/gi,
  /свинья/gi,
  /похудей/gi,
  /сбрось вес/gi,
  // ... ещё 20-25
];
```

---

## 💰 Монетизация (только Stars)

**Продукты:**
1. **Premium месяц** — 300 Stars (~200₽)
2. **Супер-интерес** — 50 Stars (~35₽) [опционально в MVP]

**Флоу оплаты:**
1. Юзер нажимает `⭐ Оформить Premium`
2. `bot.api.sendInvoice()` с `currency: "XTR"`, `prices: [{label: "Premium", amount: 300}]`
3. Обработка `pre_checkout_query` → `answerPreCheckoutQuery(true)`
4. `successful_payment` → записываем в БД `premiumUntil = now + 30 days`
5. Уведомление: "✅ Premium активирован до [дата]"

**Хранение:**
- Таблица `Payment` (лог всех транзакций)
- Поле `User.premiumUntil` (активна ли подписка)

---

## 📊 Метрики (минимум)

**Дашборд админа (текстом):**
```
📊 Статистика за всё время:
👥 Пользователей: 127
📝 Анкет заполнено: 98 (77%)
✔️ Верифицированных: 45 (46%)
💬 Сообщений: 1,234
🤝 Матчей: 67
⭐ Премиум активных: 12
💵 Выручка: 3,600 Stars (~2,400₽)
⚠️ Жалоб открытых: 3
```

**Метрики для отслеживания:**
- Конверсия онбординг → анкета
- Среднее время до первого сообщения
- Доля матчей от просмотров
- ARPPU (выручка / платящих)
- Жалобы на 100 сообщений

---

## 🚀 Дорожная карта разработки

**Неделя 1: База**
- [x] Инициализация проекта (Node + grammy + Prisma)
- [x] Схема БД + миграции
- [x] Онбординг FSM (роль → цели → город → возраст)
- [x] Сохранение начальных данных

**Неделя 2: Анкеты**
- [x] FSM создания анкеты (фото → текст → интересы)
- [x] Просмотр своей анкеты
- [x] Редактирование полей
- [x] Валидация (мин. 1 фото, возраст 18+)

**Неделя 3: Поиск и чат**
- [x] Простой поиск с фильтрами
- [x] Скоринг профилей
- [x] Inline-карточки
- [x] Лайки и матчи
- [x] Базовый чат (текст + фото)
- [x] Лимиты сообщений

**Неделя 4: Безопасность и платежи**
- [x] Верификация фото (админ-очередь)
- [x] Жалобы + модерация
- [x] Стоп-слова
- [x] Интеграция Stars для Premium
- [x] Админ-панель (стат + модерация)
- [x] Rate limits

**Неделя 5: Тестирование и запуск**
- [x] Тесты основных флоу
- [x] Нагрузочное тестирование (100+ юзеров)
- [x] Фикс багов
- [x] Документация для админов
- [x] Бета-тест с 20-30 реальных пользователей
- [x] Публичный запуск

---

## 🎯 Критерии готовности к запуску

✅ **Функционал:**
- Регистрация за 30 сек
- Создание анкеты за 2 мин
- Поиск работает с фильтрами
- Чат доставляет сообщения
- Премиум покупается и активируется
- Жалобы доходят до админа

✅ **Безопасность:**
- Стоп-слова блокируют токсичность
- Баны работают
- Rate limits не дают спамить

✅ **Производительность:**
- Поиск < 2 сек на 1000 анкет
- Отправка сообщения < 1 сек
- БД не падает при 100 одновременных юзеров

---

## 📝 Тексты (примеры)

**Приветствие:**
```
Привет! 👋

Это 💙 Твоя Линия — место, где ценят тебя такой, какая ты есть.

Никакого шейминга. Никакой токсичности. 
Только взаимное уважение и комфортное общение.

Давай подберём знакомства под твои цели — быстро и безопасно!

⚠️ Бот только для 18+. Продолжая, ты подтверждаешь свой возраст.
```
Кнопки: `[ ✅ Мне есть 18 ] [ ❌ Выход ]`

**Заполнение анкеты:**
```
📸 Загрузи от 1 до 3 фото. Первое будет главным.

💡 Совет: фото с улыбкой получают на 40% больше откликов!
```

**Правила (при старте):**
```
🛡️ Наши правила просты:

✅ Уважение и доброжелательность
✅ Честные фото и информация
✅ Соблюдение личных границ

❌ Шейминг внешности/веса = бан
❌ Домогательства = бан
❌ Спам/реклама = бан

Нарушения → мгновенная блокировка.
```

**Матч:**
```
🎉 У вас взаимный интерес с [Имя]!

Теперь можете свободно общаться. Начните с приветствия!
```

---

## 🔧 .env (пример)

```env
# Bot
BOT_TOKEN=123456:ABC-DEF...
BOT_USERNAME=tvoyalinia_bot
ENVIRONMENT=production

# Admin
ADMIN_IDS=111111111,222222222

# Database
DATABASE_URL="file:./data/database.db"

# Stars prices (in stars)
PREMIUM_MONTH_PRICE=300
SUPER_LIKE_PRICE=50

# Limits
MAX_MESSAGES_PER_HOUR=10
MAX_MESSAGES_PER_HOUR_PREMIUM=50
MAX_PROFILE_VIEWS_PER_HOUR=20
MAX_REPORTS_PER_DAY=3

# Features (toggle on/off)
VERIFICATION_ENABLED=true
SUPER_LIKE_ENABLED=false
```

---

## 🧪 Тестирование

**Ручное тестирование (чек-лист):**
- [ ] Регистрация нового пользователя
- [ ] Заполнение анкеты (все поля)
- [ ] Поиск других анкет
- [ ] Отправка сообщения до матча (лимит)
- [ ] Взаимный лайк → матч → чат
- [ ] Покупка Premium через Stars
- [ ] Отправка жалобы
- [ ] Админ: модерация жалобы
- [ ] Админ: верификация фото
- [ ] Бан пользователя

**Автотесты (опционально):**
- Unit-тесты для scoring.js, geo.js
- Интеграционные тесты для FSM

---

## 📈 План после MVP

**Если гипотеза подтвердится (100+ юзеров, 10+ платежей):**

1. **M1 (месяц 2):**
   - Подборки: "Семья", "Свидания сегодня"
   - Гео-буст
   - Супер-интерес
   - Улучшенная верификация

2. **M2 (месяц 3):**
   - YooKassa для карт
   - ML-модерация токсичности
   - "Кто смотрел меня"
   - Icebreakers

3. **M3 (месяц 4):**
   - Режим "призрак"
   - Реферальная программа
   - Промокоды
   - Веб-версия (Web App)

---

## 💡 Советы по запуску

1. **Начни с закрытой беты:** 20-30 знакомых из целевой аудитории
2. **Собери обратную связь** после 1-2 недель использования
3. **Итерируй быстро:** фиксируй баги и неудобства каждые 2-3 дня
4. **Не трать на рекламу** до подтверждения product-market fit
5. **Мониторь метрики:** конверсия в анкету, retention через неделю, первая покупка

---

## ❓ FAQ для разработки

**Q: Почему SQLite, а не PostgreSQL?**  
A: Для 1000 пользователей SQLite достаточно. Миграция на PostgreSQL — 1 день работы, когда станет узким местом.

**Q: Как хранить фото?**  
A: Telegram CDN. Храним только `file_id`. Не скачиваем на сервер (экономим место и траффик).

**Q: Что если юзер удалил профиль?**  
A: Soft delete: `isDeleted = true`, данные хранятся 30 дней (комплаенс), потом удаляются крон-джобом.

**Q: Как тестировать Stars в dev?**  
A: Telegram не даёт test-environment для Stars. Используй мок-обработчик или тестируй на production с минимальными суммами.

**Q: Нужен ли сервер?**  
A: Да. VPS от 5$/мес (Hetzner, DigitalOcean) достаточно для MVP. Или Railway/Render (бесплатный tier на старте).

---

## ✅ Итого: MVP за 4 недели

**Что получаем:**
- ✅ Работающий бот с онбордингом
- ✅ Анкеты + поиск + чат
- ✅ Базовая безопасность (стоп-слова, жалобы, баны)
- ✅ Верификация фото
- ✅ Монетизация через Stars
- ✅ Админ-панель

**Чего НЕТ (добавим после валидации):**
- ❌ YooKassa
- ❌ ML-модерация
- ❌ Множество фич из полного ТЗ

**Бюджет времени:**
- 1 разработчик full-time: 4 недели
- 1 разработчик part-time (20ч/нед): 8 недель

**Технический долг: минимальный.** SQLite + Prisma легко мигрируют на PostgreSQL. Архитектура чистая и расширяемая.

---

**Готов приступить к разработке MVP?** 🚀
